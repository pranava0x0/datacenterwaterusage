# Dependencies are pinned with == to exact versions for supply-chain safety:
# a floating >= range silently pulls whatever patch release exists at the next
# clean install, which is a supply-chain entry point. These versions are the
# ones the current test suite (419+) passes against.
#
# NOTE: == pins still can't detect a same-version re-publish on PyPI. To close
# that gap, generate a hash-locked file — `pip-compile --generate-hashes` or
# `uv lock` — and install with `pip install --require-hashes`. Tracked in
# backlog.md (Security & Supply-Chain Hygiene).

# Browser automation
playwright==1.62.0

# HTML parsing
beautifulsoup4==4.15.0
lxml==6.1.2

# HTTP
httpx==0.28.1

# PDF extraction
pdfplumber==0.11.10
PyMuPDF==1.28.2

# Excel/CSV
openpyxl==3.1.5
pandas==3.0.5

# Rate limiting / retries
tenacity==9.1.4

# Data storage
aiosqlite==0.22.1

# Logging
structlog==26.1.0

# CLI
click==8.4.2

# Dashboard
streamlit==1.62.0
plotly==6.9.0
streamlit-js-eval==1.0.0

# Static site build (build_site.py pre-renders the dashboard to static HTML
# for GitHub Pages; markdown converts the few markdown blobs at build time).
markdown==3.10.3

# Testing
pyyaml==6.0.3
pytest==9.1.1
pytest-asyncio==1.4.0
